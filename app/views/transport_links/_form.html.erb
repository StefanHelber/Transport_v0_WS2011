<%= form_for(@transport_link) do |f| %>
  <% if @transport_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transport_link.errors.count, "error") %> prohibited this transport_link from being saved:</h2>

      <ul>
      <% @transport_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>





  <div class="field">
    <%= f.label :source_id %><br />
    <%= f.collection_select :source_id,  Source.find(:all), :id, :codename %>
  </div>
  <div class="field">
    <%= f.label :sink_id %><br />
    <%= f.collection_select :sink_id,  Sink.find(:all), :id, :codename%>
  </div>
  <div class="field">
    <%= f.label :unit_cost %><br />
    <%= f.text_field :unit_cost %>
  </div>
  <div class="field">
    <%= f.label :transport_quantity %><br />
    <%= f.text_field :transport_quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
